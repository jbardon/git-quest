{
    "scripts": {
        "start": "gitbook serve --open",
	 "deploy": "gitbook build && gh-pages -d _book"
        "deploy:script": "./scripts/deploy.sh"
    },
    "devDependencies": {
        "gh-pages": "2.0.1",
        "gitbook-cli": "2.3.2"
    }
}
